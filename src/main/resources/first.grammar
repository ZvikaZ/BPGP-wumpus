# TODO allow more bts with something like: ...
# <start> ::= <bt> | (Cons <bt> <start>)
# .. but for now, let's be simpler:
<start> ::= (RegisterBThread <bt>)
<bt> ::= <request> | (Cons <wait-for> <request>) | (Cons3 <wait-for> <wait-for> <request>)
# TODO...
<request> ::= (Request)
<wait-for> ::= (WaitFor)
#TODO block?